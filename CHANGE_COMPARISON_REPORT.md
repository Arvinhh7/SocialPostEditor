# 本地未上传改动对比与审核报告

## 1. 对比基线与结论

- GitHub 仓库：`Arvinhh7/SocialPostEditor`
- 远端基线：`origin/main`，提交 `ebd3334`（`feat: add evidence-bound review, evals, traces, and human approval`）
- 本地状态：所有改动仍在工作区，尚未提交、尚未上传
- 审核结论：两批体验优化均可运行；检索负反馈、文章停用/恢复、反馈批量决策和角色克隆已通过回归测试

远端版本已经具备可追踪、可评测和人工审批能力；本地新版继续解决“未经确认的反馈污染长期记忆”以及“逐篇上传和逐字段标注过于繁琐”。

## 2. 核心变化对比

| 模块 | GitHub 旧版 | 本地新版 | 修改原因 |
|---|---|---|---|
| 反馈生命周期 | 创建后立即参与 RAG | `CANDIDATE → ADMITTED / REJECTED` | 区分稳定偏好与一次性修改，防止反馈污染 |
| Agent 反馈检索 | 检索当前角色的全部反馈 | 只检索当前角色的 `ADMITTED` 反馈 | 在相关性排序前执行硬过滤 |
| 决策审计 | 没有准入决策记录 | 保存动作、前后状态、原因和时间 | 防止状态被静默覆盖，便于追责和复盘 |
| 数据库升级 | 反馈表没有准入字段 | 旧反馈迁移为 `ADMITTED`，新反馈默认为 `CANDIDATE` | 保持旧数据行为，同时收紧未来写入 |
| 文章投喂 | 单文件上传，元数据需要逐项填写 | 最多 30 个文件批量上传，本地自动推断常见元数据 | 降低重复操作和人工填写错误 |
| 重复与纠错 | 重复内容可反复入库，导入后缺少轻量纠错入口 | 规范化正文指纹去重；`PATCH /posts/{post_id}` 可纠正字段 | 避免语料偏置，错误无需重新上传正文 |
| 生成参数 | 每次重复填写平台、受众、语气和禁用词 | 角色保存默认任务参数，请求只覆盖变化字段 | 降低重复填写和配置漂移 |
| 审批列表 | 需要打开生成详情才能看到审核原因 | 列表直接返回结构化 `review_summary` | 减少审批跳转并快速定位改写原因 |
| 批量纠错 | 只能逐篇 PATCH | 按角色和 ID 列表原子批量修改 | 大批量导入后的统一纠错更高效 |
| 检索负反馈 | 只能通过改终稿间接反馈 | 生成记录保存 `NOT_RELEVANT`；文章接口独立执行 `RETIRE/RESTORE` | 分开任务级相关性与全局文章生命周期 |
| 候选反馈处理 | 只能逐条 ADMIT/REJECT | 同角色最多 100 条原子批量决策 | 减少重复操作且避免部分完成 |
| 角色复用 | 新角色重复填写规则 | 克隆身份规则和任务默认值，不复制记忆 | 支持同一作者的多平台配置 |
| 验证 | 35 个测试，冒烟受本地模式影响 | 57 个测试；冒烟强制 Mock | 覆盖两批体验优化、入口一致性、事务和状态边界 |

## 3. 文件级修改与原因

- `app/agent.py`：反馈检索增加 `ADMITTED` 硬过滤，并把 `feedback_id` 写入上下文。原因是候选或拒绝经验不能影响生成，且命中来源需要可追踪。
- `app/db.py`：增加反馈准入状态、决策动作表和旧反馈兼容迁移。原因是准入必须可审计，同时不能突然改变升级前已确认反馈的行为。
- `app/main.py`：增加反馈查询、准入/拒绝和准入历史接口。原因是让人工经验门禁可以实际操作。
- `app/models.py`：增加反馈准入决策模型，拒绝时必须说明原因。
- `app/ingestion.py`：增加无需模型调用的标题、平台、语言、类型、主题、语气、日期推断和正文指纹。
- `app/main.py`、`app/db.py`：增加批量上传和文章元数据修改接口，单文件/单条旧接口继续保留。
- `app/models.py`、`app/db.py`、`app/main.py`：增加角色默认生成参数、角色更新接口和三层参数合并逻辑。
- `app/db.py`：审批列表增加审核/改写次数、累计问题和可展示摘要；文章批量修正采用同角色原子事务。
- `tests/test_feedback_admission.py`：新增 5 项测试，覆盖候选隔离、准入、拒绝、终态、旧库迁移和 API。
- `tests/test_ingestion.py`：新增 5 项测试，覆盖自动标注、日期解析、规范化去重、批量上传和元数据纠正。
- `tests/test_experience_improvements.py`：新增 4 项测试，覆盖请求覆盖优先级、角色默认值清除、审批摘要和批量修改回滚。
- `tests/test_second_batch_improvements.py`：新增 4 项测试，覆盖角色克隆隔离、反馈批量原子性、检索来源校验和文章停用/恢复。
- `scripts/smoke_test.py`：强制使用 Mock 并兼容 Windows 中文输出，避免本地 `.env` 使冒烟测试意外消耗真实 API。
- `README.md`、`RAG_DESIGN.md`、`PRODUCT_IMPROVEMENT_REPORT.md`：同步实现状态、使用步骤和 RAG 过滤边界。

## 4. 各 Agent/组件当前运行逻辑

### 4.1 Profile Agent

1. 读取当前 `role_id` 下真实性不低于 4 的历史文章。
2. 最多选取 30 篇样本，文章内容截断后交给模型。
3. 只总结中英文价值观、语气、开头、节奏、CTA 和避用表达，不推测身份经历。
4. 每次重建生成新的画像版本，并记录样本文章 ID。

### 4.2 RAG Retriever

1. 只读取当前角色的文章，保持角色数据隔离。
2. 查询由主题、格式、语气、目标、受众、平台和语言组成。
3. 混合分数保持旧版算法：字符 n-gram TF-IDF 60%、关键词 18%、元数据 14%、真实性 6%、时效性 2%。
4. 元数据内部继续比较平台、语言、内容格式和语气。
5. 使用 MMR：相关性 82%、重复惩罚 18%，避免近似文章占满全部位置。
6. 返回每个分数组成和命中原因；本次没有引入向量数据库或 embedding。

RAG 排序算法本身没有改动，变化发生在“如何使用和验证检索结果”：参考文章 ID 被写入 Evidence Contract 和运行轨迹；历史文章被明确限定为风格与判断参考，不自动成为事实依据；新增固定相关文章 ID 的 Precision@K、Recall@K、MRR 评测。

反馈 RAG 另有一道准入过滤：只读取当前角色中状态为 `ADMITTED` 的反馈，再进行相关性排序；`CANDIDATE` 和 `REJECTED` 在检索前就被排除。

### 4.3 Evidence Contract Builder

1. 去重并保存 `proof_points`，形成 `required_points`。
2. 只把含数字的 proof point 记入允许数字依据。
3. 保存禁用表达、角色身份规则、平台、语言、格式和参考文章 ID。
4. 固定要求：无依据数字为 0、禁用词为 0、与单篇历史文章的整体相似度不超过 0.72。
5. 合同由本地规则生成，不增加模型调用。

### 4.4 Draft Agent

1. 接收任务、角色、画像、RAG 文章、相关人工反馈和 Evidence Contract。
2. 历史文章只学习判断方式、节奏和语气，不允许逐句仿写。
3. 不允许增加合同之外的数字、客户、案例、结果或亲历。
4. 输出正文后立即把调用结果、模型、Prompt 版本和耗时写入运行轨迹。

### 4.5 Independent Reviewer

1. 先接收本地确定性问题：无依据数字、禁用词、复制风险和正文过短。
2. 再由只读模型审核角色一致性、任务完成度、模板感、事实、宣传强度和 CTA。
3. 本地确定性问题优先级高于模型；模型返回 PASS 也不能覆盖规则问题。
4. 有问题返回 `REVISE`；无问题返回 `PASS`。
5. 模型失败、空输出、非法 JSON、非法 verdict 或模型自行返回 BLOCKED 时，由程序生成可信的 `BLOCKED`。
6. Reviewer 不持有数据库和人工审批权限，不能修改正文或批准发布。

### 4.6 Revision Agent

1. 只在 Reviewer 返回 `REVISE` 时运行。
2. 只修复列出的问题，保留原稿中合格的观点和语气。
3. 继续受同一 Evidence Contract 约束，不得在改稿时引入新事实。
4. 最多执行配置允许的重写轮数；`PASS` 或 `BLOCKED` 都会停止循环。

### 4.7 Deterministic Eval Runner

1. 只评测已保存且状态为 PASS/REVISE 的完整生成，不评测 FAILED/BLOCKED 半成品。
2. 根据人工标注的相关文章 ID 计算 Precision@K、Recall@K 和 MRR。
3. 按数字 token 精确检查正文数字是否出现在 proof points 中，避免旧版字符串包含判断的误放行。
4. 小红书结构评分读取真实生成正文，而不是 expected_data 中的示例：标题 1–40 字、正文 80–2000 字、至少两段、1–10 个标签，四项通过三项即达 0.75 阈值。
5. 比较功能只允许同一 Eval Case、同一指标集合和版本，输出相对基线差值。

### 4.8 Human Approval

1. PASS 或达到最大改稿轮数后的 REVISE 内容进入 `PENDING_REVIEW`。
2. 人可以 EDIT、APPROVE 或 REJECT；每次操作保存修改前后文本、diff、原因和时间。
3. 编辑已批准内容会重新进入待审核。
4. REJECTED、FAILED、BLOCKED 内容不能直接批准；被拒绝内容不能通过 EDIT 绕过拒绝状态。
5. 当前系统仍不自动发布社交媒体内容。

### 4.9 Feedback Admission

1. `POST /feedback` 创建的反馈默认进入 `CANDIDATE`，此时不会参与 Agent 生成。
2. 人工通过 `ADMIT` 把稳定、可复用的修改经验转成 `ADMITTED`；一次性或错误修改通过 `REJECT` 排除。
3. 每次决策保存原状态、结果状态、原因和时间；一旦决策不能直接覆盖，避免静默改写历史。
4. 旧版本已经存在的反馈迁移为 `ADMITTED`，保持升级兼容；新反馈必须显式确认。

## 5. 审核中发现并修复的问题

1. 小红书评测曾可能读取期望示例正文，造成生成内容很差但结构分仍通过；已改为只评分真实 `final_text`。
2. REJECTED 内容曾可通过 EDIT 重新进入待审核；已收紧状态转换。
3. 模型自行返回 `BLOCKED` 曾可能被后处理改成 PASS；已改为程序控制的阻塞结论。
4. FAILED/BLOCKED 生成曾可进入离线评测；已限制为只评测完整输出。
5. 新增旧版数据库迁移测试，确认旧终稿不会丢失，并正确进入待审核状态。

## 6. 测试与安全结果

- 单元测试：57 项通过
- 端到端 Mock 冒烟：通过，画像、RAG、生成、审核和保存完整跑通
- Python 编译检查：通过
- FastAPI OpenAPI 构建：通过，36 条路由、26 个路径、21 个 Schema
- `git diff --check`：通过
- `.env`：本地和 GitHub 均未被 Git 跟踪，且受 `.gitignore` 保护
- API Key 扫描：未在待上传代码和文档中发现疑似明文密钥
- 未执行真实 OpenAI/DeepSeek 在线调用，避免产生费用；上线前仍应单独进行一次受控 live smoke test

## 7. 仍未解决的限制

- Reviewer 已实现程序权限隔离，但当前仍与 Draft 共用同一模型供应商，尚未验证异构模型复核收益。
- 反馈经验准入已在后续改进中实现：新反馈默认 `CANDIDATE`，只有 `ADMITTED` 会进入 Agent 检索，`REJECTED` 永久排除，并保存人工决策记录。
- RAG 的“语义分”仍是字符 n-gram TF-IDF，不是真正 embedding；应先积累固定评测集，再决定是否升级。
- 当前确定性评测覆盖检索、数字事实和小红书结构，尚未实现稳定的人物风格评分与人工接受度统计。
- 运行步骤记录模型、Prompt 和耗时，但尚未记录 token 与估算费用。
- 当前只有 `role_id` 隔离，没有用户登录和租户授权，不适合直接作为多用户公网服务。
- 批量投喂的元数据推断是低成本本地启发式规则；不确定的平台会留空并提示，重要分类仍应抽样检查。

## 8. 上传前建议

建议把本批改动作为一个可回滚的功能提交，提交信息可使用：

```text
feat: add evidence-bound review, evals, traces, and human approval
```

反馈准入、批量投喂和两批体验优化均已完成。下一步应先用真实数据观察误召回、停用文章和批量审批使用情况；语义检索升级仍应等待固定评测数据。
